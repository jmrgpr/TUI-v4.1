# CANONICAL_DATASET_EXTENDED_JSON_v12 (placeholder)

Este archivo será generado cuando existan outputs v12.

Regla:
- Los JSON por run no se versionan.
- Se publican hashes `sha256` y rutas para auditoría.

Estado: **no generado**.

