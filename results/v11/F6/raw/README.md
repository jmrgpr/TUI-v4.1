# raw (F6)

Outputs crudos generados por `sim.prototipo_rl_simbiosis` (JSON + CSV agregado por run).

- Estos archivos **no** son canónicos y **no** deben versionarse (ver `.gitignore`).
- Para obtener outputs canónicos por agente (`*_episodes.csv` por agente), ejecutar:

```powershell
python scripts/organize_F6_results.py
```

