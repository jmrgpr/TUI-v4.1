# F4 raw / F2_redteam

Salidas crudas por stakes:

- `stkL/` (low-stakes)
- `stkH/` (high-stakes, budget `B=3`)

Cada subcarpeta contiene runs por `seed×grid×pgf_mix` (el budget va en el nombre como `b3`).
