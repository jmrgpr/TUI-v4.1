# F8 / raw (local-only)

Esta carpeta contiene outputs **raw** (JSON + CSV por corrida) y **no se versiona** para evitar inflar el repo.  
El dataset canónico se genera con `scripts/organize_F8_results.py` y vive en `results/v11/F8/F2_redteam/...`.

